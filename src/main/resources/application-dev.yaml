spring:
  data:
    mongodb:
      uri: mongodb+srv://kaisilious:***REDACTED***@journal.gnjq81h.mongodb.net/?retryWrites=true&w=majority&appName=Journal
      database: journaldb
      auto-index-creation: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:}
            client-secret: ${GOOGLE_CLIENT_SECRET:}

server:
  port: 8081
#  servlet:
#    context-path: /journal

app:
  oauth:
    google:
      redirect-uri: http://localhost:8081/auth/google/callback
      frontend-redirect-uri: ${GOOGLE_OAUTH_FRONTEND_REDIRECT_URI:}


