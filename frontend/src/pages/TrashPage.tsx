import JournalsListPage from './JournalsListPage';

const TrashPage = () => (
  <JournalsListPage title="Trash" subtitle="Restore or permanently delete entries." trashedOnly />
);

export default TrashPage;
