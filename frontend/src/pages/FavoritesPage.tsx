import JournalsListPage from './JournalsListPage';

const FavoritesPage = () => (
  <JournalsListPage title="Favorites" subtitle="Your starred entries, always within reach." favoritesOnly />
);

export default FavoritesPage;
